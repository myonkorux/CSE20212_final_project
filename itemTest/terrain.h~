#ifndef TREE_H
#define TREE_H

#include"item.h"

class Terrain : public Terrain
{
	public:
		Terrain(int, int, int);
	private:
		SDL_Surface * loadTerrainImage(string);
		SDL_Surface * TerrainImage;
		
};


#endif
